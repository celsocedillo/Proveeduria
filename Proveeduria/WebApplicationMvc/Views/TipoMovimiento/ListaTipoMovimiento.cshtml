@model IEnumerable<Proveduria.Models.EPRTA_TIPO_MOVIMIENTO>

@*@{
        ViewBag.Title = "ListaTipoMovimiento";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }*@


@section styles {
    <!-- DataTables -->
    @*<link rel="stylesheet" href=@Url.Content("/Content/bower_components/datatables.net-bs/css/dataTables.bootstrap.min.css")>*@
    <link rel="stylesheet" href=@Url.Content("/Content/bower_components/datatable/jquery.dataTables.css")>
    <link rel="stylesheet" href=@Url.Content("/Content/bower_components/datatable/dataTables.jqueryui.min.css")>

}


<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        Tipos de Movimientos
        <small>Control panel</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active">Dashboard</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">

    <p>
        <button class="btn btn-primary" onclick="location.href='@Url.Action("Create", "InvMedida")'">
            <i class="fa fa-plus"></i>  CREAR
        </button>
    </p>

    <!-- begin panel -->
    <div class="box box-default">
        <div class="box-header">
            <div class="panel-heading-btn">
            </div>
            <h4 class="panel-title">
                <i class="fa fa-list" aria-hidden="true"></i>  MEDIDAS
            </h4>
        </div>
        <div class="box-body no-padding">

            <table id="tabTipoMov" class="compact stripe dataTable hover">
                <thead>
                    <tr>
                        <th>
                            Id
                        </th>
                        <th>
                            Nombre
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @*@foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ID_TIPO_MOVIMIENTO)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NOMBRE)
                                </td>
                                <td>
                                    <button id="butEditar" type="button" class="btn btn-default btn-xs clsedit">
                                        <i class="fa fa-pencil"></i>
                                    </button>
                                    <button id="butEliminar" type="button" class="btn btn-default btn-xs">
                                        <i class="fa fa-trash"></i>
                                    </button>
                                </td>
                            </tr>
                        }*@
                </tbody>
            </table>

        </div>
    </div>
    <!-- end panel -->

    <div class="modal fade" id="dlgTipoMov">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    <h4 class="modal-title"><span id="titAgrActivo"></span></h4>
                </div>
                <div class="modal-body">
                    <form class="form-horizontal">
                        <div class="form-group">
                            <label class="col-sm-2 control-label">ID</label>
                            <div class="col-sm-10">
                                <input id="txtIdTipoMov" type="text" class="form-control" />
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">Nombre</label>
                            <div class="col-sm-10">
                                <input id="txtNombre" type="text" class="form-control" />
                            </div>
                        </div>

                    </form>
                </div>
                <div class="modal-footer">
                    <button id="butAcepta" type="button" class="btn btn-primary btn-sm">Grabar</button>
                    <button id="butCancela" type="button" class="btn btn-default btn-sm" data-dismiss="modal">Cancelar</button>
                </div>
            </div>
            <!-- /.modal-content -->
        </div>
        <!-- /.modal-dialog -->
    </div>


</section>

@section scripts {
    <!-- DataTables -->
    @*<script src=@Url.Content("/Content/bower_components/datatables.net/js/jquery.dataTables.min.js")></script>
        <script src=@Url.Content("/Content/bower_components/datatables.net-bs/js/dataTables.bootstrap.min.js")></script>*@
    <script src=@Url.Content("/Content/bower_components/datatable/jquery.dataTables.js")></script>
    <script src=@Url.Content("/Content/bower_components/datatable/dataTables.jqueryui.min.js")></script>
    <script type="text/javascript" src=@Url.Content("/Scripts/App/InvMedida.js")></script>
    <script type="text/javascript">
        var tabtipomov;
        var ldata = [];


        function CargaDatos()
        {
            var $this = $(this);

            $.ajax({
                    type: "POST",
                    traditional: true,
                    datatype: "json",
                    data: null,
                    contentType: 'application/json; charset=utf-8',
                    url: "/TipoMovimiento/GetListaTipoMovimiento",
                    beforeSend: function () {
                        run_waitMe($(".panel-body"), 'win8', 'Cargando...');
                    },
                    success: function (data) {
                        if (data.error) {
                            swal({
                                type: 'error',
                                text: 'Error al cargar los datos.',
                                confirmButtonColor: '#00BCD4'
                            });
                        }
                        else {
                            //var items = data.items;
                            var data = $.parseJSON(data.data);
                            //ldata = data.data;
                            //tabtipomov.ajax.reload();
                            //ldata = [{ "ID_TIPO_MOVIMIENTO": "1", "NOMBRE": "ASASAS" }, { "ID_TIPO_MOVIMIENTO": "1", "NOMBRE": "WEWEWEWE" }, { "ID_TIPO_MOVIMIENTO": "1", "NOMBRE": "KLKLKLKLKLKL" }];
                            tabtipomov.clear();
                            tabtipomov.rows.add(data);
                            tabtipomov.draw();
                            //$('#tabTipoMov').DataTable().ajax.reload();
                            //if (data.length > 0) {
                            //    var editar = `<button id="butEditar" type="button" class="btn btn-default btn-xs clsedit">
                            //                        <i class="fa fa-pencil"></i>
                            //                    </button>`;
                            //    var eliminar = `<button id="butEliminar" type="button" class="btn btn-default btn-xs">
                            //                        <i class="fa fa-trash"></i>
                            //                    </button>`;
                            //    for (var i = 0; i < data.length; i++) {
                            //        tabtipomov.row.add([
                            //            data[i].ID_TIPO_MOVIMIENTO,
                            //            data[i].NOMBRE,
                            //            editar + eliminar
                            //        ]).draw();
                            //    }
                            //}
                            //else {
                            //    swal({
                            //        type: 'info',
                            //        text: 'No se encontraron resultados con los datos proporcionados.',
                            //        confirmButtonColor: '#00BCD4'
                            //    });
                            //}
                        }
                        $(".panel-body").waitMe('hide');
                        $this.button('reset');
                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                        $(".panel-body").waitMe('hide');
                        $this.button('reset');
                        swal({
                            type: 'error',
                            text: 'Error al cargar los datos.',
                            confirmButtonColor: '#00BCD4'
                        });
                    },
                    complete: function () {
                        calcularTotales();
                    }
                });

        }


        $(document).ready(function () {

            tabtipomov = $('#tabTipoMov').DataTable({
                "paging": false,
                "searching": false,
                "filter": false,
                "dom" :'t',
                "columnDefs":
                [
                    { "targets": [0], "width": "80px", "orderable": false },
                    { "targets": [2], "width": "40px", "defaultContent": '<button id="butEditar" type="button" class="btn btn-default btn-xs clsedit"><i class="fa fa-pencil"></i></button>'}
                ],
                "data": ldata,
                "columns": [
                    { "data": 'ID_TIPO_MOVIMIENTO' },
                    { "data": 'NOMBRE' }
                ]
            });

            CargaDatos();

            tabtipomov.on('click', 'button.clsedit', function (e) {
                var row = $(this).closest('tr');
                var data = tabtipomov.row($(this).parents('tr')).data();
                var parametros = JSON.stringify(
                    {
                        //pid: row.ID_TIPO_MOVIMIENTO,
                        pid: data.ID_TIPO_MOVIMIENTO
                    });

                $.ajax({
                dataType: 'JSON',
                url: '@Url.Action("GetTipoMovimiento", "TipoMovimiento")',
                type: "POST",
                contentType: 'application/json; charset=utf-8',
                data:  parametros,
                success: function (result)
                 {
                    $("#dlgTipoMov").modal('toggle');
                    $("#txtIdTipoMov").val(result.data.ID_TIPO_MOVIMIENTO);
                    $("#txtNombre").val(result.data.NOMBRE);
                 },
                error: function (result) {
                    alert("Error :" + result.responseText);
                    $("#msgMain").html(result.responseText);
                    $("#msgMain").addClass("alert alert-danger");
                }
                });
            });


            @*$('#tabTipoMov tbody').on('click', '[id*=butEditar]', function (e) {
                $("#dlgTipoMov").modal('toggle');
                //var data = tab.row($(this).parents('tr')).data();
                var data = $("#tabTipoMov").DataTable().row($(this).parents('tr')).data();
                var parametros = JSON.stringify(
                    {
                        pid: data.ID_TIPO_MOVIMIENTO,
                        pid: data[0]
                    });

                $.ajax({
                dataType: 'JSON',
                url: '@Url.Action("GetTipoMovimiento", "TipoMovimiento")',
                type: "POST",
                contentType: 'application/json; charset=utf-8',
                data:  parametros,
                success: function (result)
                 {

                    $("#txtIdTipoMov").val(result.data.ID_TIPO_MOVIMIENTO);
                    $("#txtNombre").val(result.data.NOMBRE);
                 },
                error: function (result) {
                    alert("Error :" + result.responseText);
                    $("#msgMain").html(result.responseText);
                    $("#msgMain").addClass("alert alert-danger");
                }
                });

            });*@
        });


    </script>
}